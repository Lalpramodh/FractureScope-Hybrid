FractureScope model contract

The only supported inference model is yolov8_model.pt. Keep this file in the project root or set YOLO_MODEL_PATH to an alternative YOLO weights path. Each Gunicorn worker loads one model during application startup and reuses it for CPU inference.
