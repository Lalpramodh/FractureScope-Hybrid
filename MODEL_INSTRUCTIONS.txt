FractureScope model contract

The only supported inference model is yolov8_model.pt. Keep this file in the project root or set YOLO_MODEL_PATH to an alternative YOLO weights path. The model is loaded lazily when /predict is called and runs on CPU.
